@model BadHomburg.Models.Person

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>Person</legend>

    <div class="display-label">Anrede</div>
    <div class="display-field">@Model.Anrede</div>

    <div class="display-label">Vorname</div>
    <div class="display-field">@Model.Vorname</div>

    <div class="display-label">Nachname</div>
    <div class="display-field">@Model.Nachname</div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id=Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>

